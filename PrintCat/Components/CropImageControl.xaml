<UserControl x:Class="PrintCat.Components.CropImageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="250">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition Height="Auto"></RowDefinition>
    </Grid.RowDefinitions>
    <Label Content="剪裁" Grid.Row="0" Background="{DynamicResource {x:Static SystemColors.ActiveCaptionBrushKey}}" Margin="0,0,0,10"/>
    <DockPanel Grid.Row="1" HorizontalAlignment="Right">
      <Button Content="剪裁" x:Name="CropBtn" Width="100" Height="30" FontSize="14" IsEnabled="false" Click="CropBtn_Click"/>
    </DockPanel>
    <StackPanel Grid.Row="2" Margin="0,10,0,96">
      <Button Content="4X6" x:Name="crop4x6Btn" Height="30" FontSize="14" IsEnabled="False" Click="crop4x6Btn_Click"/>
    </StackPanel>
  </Grid>
</UserControl>
